{% extends 'base.html.twig' %}


{% block body %}

<div class="container my-5">
    <h1>Espace membre</h1>    

    <div class="row">
        <div class="col-md-3">
            {% include 'account/_menu.html.twig' %}
        </div>
        <div class="col-md-9">
            <h5>Liste d'envies</h5>
            {% if app.user.wishlist|length > 0 %}
                {% for product in app.user.wishlist %}
                    {{dump(product)}}
                {% endfor %}
            {% else %}
                Vous n'vez pas de produit dans votre liste
            {% endif %}
        </div>
    </div>

</div>

{% endblock %}
